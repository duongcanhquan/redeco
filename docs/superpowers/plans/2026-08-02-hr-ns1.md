# HR NS1 Core — Implementation Plan

> **For agentic workers:** COMPLETE ALL TASKS. Blueprint đã duyệt.

**Goal:** Module `nhan-su` NS1 — departments, employees, contracts + hub UI.

**Tech:** Supabase migration/RLS, `@optimake/domain` types, `hr.service.ts`, App Router UI.

## Tasks

1. Migration `20260802220000_hr_module_ns1.sql` + apply
2. Domain HR types + export
3. `hr.service.ts` + actions
4. Nav tabs + layout `/hr`
5. UI pages hub / departments / employees / [id]
6. `entitle-demo-hr.cjs` + smoke + current-state + blueprint status
